"use client";

import Button from "../button";

export default function FloatMenu() {
  return (
    <Button
      label="구독신청"
      onClick={() => {}}
      className="fixed bottom-[calc(20%)] -right-8 lg:-right-10 rotate-270 py-2.5 pl-12.5 pr-5 px-0 bg-primary text-white rounded-t-xl"
      aria-label="float menu button"
    />
  );
}
